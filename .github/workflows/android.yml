name: SOCIAL SPARK APP

env:
  # The name of the main module repository
  main_project_module: Social Spark

  # The name of the Play Store
  playstore_name: SOCIAL SPARK
  
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'gradle'
        cache-dependency-path: 'VIRAAT NAIDOO IMAD ASSIGNMENT 1/**/*.gradle*'
        

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      working-directory: './VIRAAT NAIDOO IMAD ASSIGNMENT 1'
      
    - name: Accept Android SDK licenses
      run: yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses
      working-directory: './VIRAAT NAIDOO IMAD ASSIGNMENT 1'

      

    - name: Build with Gradle
      run: ./gradlew build
      working-directory: './VIRAAT NAIDOO IMAD ASSIGNMENT 1'


    - name: Run tests
      run: ./gradlew test
      working-directory: './VIRAAT NAIDOO IMAD ASSIGNMENT 1'

